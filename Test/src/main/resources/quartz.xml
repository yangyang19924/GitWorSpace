<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

    <bean id="quartzJob" class="com.yangyang.quartzJob.QuartzJob"></bean>
    <bean id="quartzJobDetail"
          class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <!-- 指定任务类 -->
        <property name="targetObject" ref="quartzJob" />
        <!-- 指定任务执行的方法 -->
        <property name="targetMethod" value="execute" />
    </bean>
    <!--触发器-->
    <bean id="quartzJobTrigger"
          class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
        <property name="jobDetail" ref="quartzJobDetail" />
        <!-- 每10秒运行一次 -->
        <property name="cronExpression" value="0/10 * * * * ?" />
    </bean>

    <bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <property name="triggers">
            <list>
                <ref bean="quartzJobTrigger" />
                <ref bean="TestQuartz"/>
            </list>
        </property>
    </bean>


    <!--配置定时任务-->
    <bean id="TestQuartzJob" class="org.springframework.scheduling.quartz.JobDetailFactoryBean">
        <property name="jobClass" value="com.yangyang.quartzJob.TestQuartzJob"></property>
    </bean>
    <bean id="TestQuartz" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
        <property name="jobDetail" ref="TestQuartzJob" />
        <!--里面的参数一般是6个参数依次代表秒，分，时，天，月，年.支持如?(无限制),*（任意）号等匹配符号-->
        <property name="cronExpression" value="0/10 * * * * ?" />
    </bean>
</beans>